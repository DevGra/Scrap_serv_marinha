# -------------------------------  SELENIUM ----------------------------------------
#browser = webdriver.Chrome()
#browser.get(base_url)
#import pdb; pdb.set_trace()

# Agora podemos encontrar o elemento
#botao = firefox.find_element_by_id('<botao_id>')
#botao = browser.find_element_by_id('lista_next')
# E podemos manipulá-lo :)
#import pdb; pdb.set_trace()
#botao.click()
#import pdb; pdb.set_trace()

#def esperar_pelo_elemento(chrome):
  #return chrome.find_element_by_id('<elemento_id>')

#elemento = WebDriverWait(Chrome, 5).until(esperar_pelo_elemento)

# ------------------------------------------FIM SELENIUM ------------------------------------------
